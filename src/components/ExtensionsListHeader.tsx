import Typography from '@/components/Typography';

export default function () {
	return (
		<div className='mb-400 flex flex-col items-center md:flex-row'>
			<Typography variant='h1'>Extensions List</Typography>
		</div>
	);
}
